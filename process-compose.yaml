version: "0.5"

processes:
  postgres:
    command: |
      mkdir -p "$(pwd)/data/postgres/locks"
      postgres -D ./data/postgres -k "$(pwd)/data/postgres/locks"
  nats:
    command: nats-server -m 8222
  etcd:
    command: etcd